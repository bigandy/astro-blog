---
import DefaultLayout from '@layouts/DefaultLayout.astro';
type AvailableLanguage = "en" | "fr"
const locale: AvailableLanguage = Astro.currentLocale as AvailableLanguage ?? 'en'; 
const copy = {
	en: {
		title: '404',
		message: 'Page cannot be found',
	},
	fr: {
		title: '404',
		message: 'Page non trouvée',
	},
};
---

<DefaultLayout title={copy[locale].title}>
	<p>{copy[locale].message}</p>
</DefaultLayout>

<style>
body {
	--theme-accent: #ea3c3c;
}
</style>