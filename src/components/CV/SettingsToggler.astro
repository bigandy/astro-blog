---
// import LanguageSwitcher from "@components/Nav/LanguageSwitcher.astro";
import ThemeSwitcher from "@components/Nav/ThemeSwitcher.astro";

interface Props {
	src: string;
	alt: string;
	dimension?: number;
    showThemeSwitcher?: boolean;
}
const { src, alt, dimension = 100, showThemeSwitcher } = Astro.props;
---

<div class="settings-toggler">

    {showThemeSwitcher && (<ThemeSwitcher><img {src} {alt} height={dimension} width={dimension} /></ThemeSwitcher>)}

    {!showThemeSwitcher && (<img {src} {alt} height={dimension} width={dimension} />)}
    <!-- <LanguageSwitcher /> -->
</div>

<style>
    .settings-toggler {
        margin-inline-start: auto;
    }
    img {
        anchor-name: --me;
        border-radius: 50%;
        display: block;
    }

    :global(.language-switcher) {
        position: fixed;
        position-anchor: --me;
        position-area: bottom;
        z-index: 1;
        inset: 40px 0;
    }

    @media print {
        img {
            filter: none !important;
        }
    }
</style>
